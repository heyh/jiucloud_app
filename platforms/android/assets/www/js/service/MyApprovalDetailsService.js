/**
 * Created by heyh on 16/8/2.
 */

angular.module('MyApprovalDetails.services', ['util.http', 'util.localStorage'])
    .service('MyApprovalDetailsService', function ($window, $q, http, localStorage, $ionicPopup, $ionicLoading, $translate, reqConfig, $interval, $log, $state, $timeout,
                                             $ionicViewSwitcher, configUtil) {
        return {}
    });
